import React from 'react'

function CustomEmptyStateIndicator() {
  return (
    <div style={{ width: '100%' }}>
      <div>You have no active chats currently.</div>
      <div>Go to Errands page and start a convatation.</div>
    </div>
  )
}

export default CustomEmptyStateIndicator